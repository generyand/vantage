{
  "buildCommand": "pnpm generate-types && cd apps/web && pnpm build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "pnpm install --frozen-lockfile",
  "devCommand": "cd apps/web && pnpm dev",
  "framework": "nextjs",
  "rootDirectory": "apps/web"
}
