{
  "name": "@vantage/shared",
  "version": "0.1.0",
  "description": "Shared types and utilities for Vantage applications",
  "private": true,
  "main": "./src/generated/index.ts",
  "types": "./src/generated/index.ts",
  "scripts": {
    "generate": "cd ../../ && pnpm generate-types",
    "type-check": "tsc --noEmit --skipLibCheck"
  },
  "peerDependencies": {
    "@tanstack/react-query": "^5.0.0"
  },
  "devDependencies": {
    "@tanstack/react-query": "^5.80.7",
    "typescript": "^5.0.0"
  }
} 